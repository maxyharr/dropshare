.center.hero-unit
  %h1 Feed
  %p= current_user.name + " is logged in"
  .well 
    %h2 Facebook friends using DropShare
    -@friends.each do |friend|
      =image_tag friend.picture
      =link_to friend.name, user_path(User.find_by_uid(friend.identifier))
      %br