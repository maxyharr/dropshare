.center.hero-unit
  %h1 Feed
  %p= current_user.name + " is logged in"
  .well 
    %h2 Facebook friends using DropShare
    .table.table-bordered.table-striped
      %thead
        %th FRIEND
      - @friends.each do |friend|
        - if User.find_by_uid(friend.identifier)
          %tr
            %td= image_tag friend.picture
            %td= link_to friend.name, user_path(User.find_by_uid(friend.identifier).id)
  